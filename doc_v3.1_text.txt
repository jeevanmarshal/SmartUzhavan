SmartUzhavanà®•à®¿à®°à®¾à®® à®•à®£à®•à¯à®•à¯ à®®à¯‡à®²à®¾à®£à¯à®®à¯ˆ à®…à®®à¯ˆà®ªà¯à®ªà¯VERSION 3.1 â€” IMPLEMENTATION PLAN DOCUMENTFeature Expansion  |  Module Restructure  |  Logic Redesign  |  PDF ReportsProjectSmartUzhavan â€” Rural Ledger & Machinery SystemVersion3.1 â€” Feature Expansion & RestructureBased OnSmartUzhavan_v3_Documentation.docx (Gold Master)Total Changes12 Changes  |  8 Files Modified  |  3 Files NewDateApril 2026This document is the authoritative implementation plan for v3.1.The Anti-Gravity Agent must read this document completely before writing any code.1. Version 3.1 OverviewVersion 3.1 is a feature expansion release built on top of the v3.0 Gold Master. The v3.0 system stabilized all crash-level issues, fixed core calculations, and established the backend PDF server. Version 3.1 expands the system's real-world usability by making financial records more flexible, reports more useful, the UI more consistent, and the module structure more logical.All 12 changes in this version come directly from real-world usage feedback. They are mapped precisely to the existing codebase â€” this is not a redesign, it is a targeted expansion.1.1  Complete Change Index#ModuleChangeFiles AffectedTypeC01Driver â€” DashboardSeason+Year & Month+Year filterDriverDashboard.jsxModifyC02Driver â€” EntryFix empty session time defaultsDriverEntry.jsxModifyC03Driver â€” SalaryDate range filter + PDF exportSalaryDriverâ†’Drivers.jsxModifyC04Admin â€” SettingsChange any driver's login PINSystem.jsx (Settings tab)ModifyC05Admin â€” ReportsPDF export for all 5 report typesSystem.jsx + serverModify + BackendC06Admin â€” ExpensesFix undefined labels + 3 source-specific category listsExpenses.jsxModifyC07Admin â€” Own FarmPaddy vs Vaikool dynamic income formOwnFarmIncome.jsxModifyC08Admin â€” DriversTwo sub-modules + salary logic decouple from logsDrivers.jsx + App.jsxMajor ExpandC09Admin â€” WorkersTwo sub-modules + multi-worker entry + PDF reportsWorkers.jsx + serverMajor ExpandC10Admin â€” NavigationMerge Config+Settings+Reports â†’ System moduleSystem.jsx + App.jsxNew File + ModifyC11Admin â€” Driver LogsAdmin edit/delete of driver log entries with cascade guardDriverEntry.jsxModifyC12Admin â€” Worker SalaryMulti-worker selection for same work on same dayWorkers.jsxModify1.2  Files SummaryFileActionChanges In This Versionsrc/pages/DriverDashboard.jsxModifyAdd season/month filter state and filtered stats computationsrc/pages/DriverEntry.jsxModifyEmpty session defaults + Admin edit/delete log with cascade guardsrc/pages/Drivers.jsxMajor ExpandAdd 2 tabs: Drivers sub-module + Salary sub-module (absorbs SalaryDriver)src/pages/SalaryDriver.jsxDeleteContent moved into Drivers.jsx Tab 2 with new salary logicsrc/pages/Expenses.jsxModifyAdd en field to all categories + 3 source-specific category mapssrc/pages/OwnFarmIncome.jsxModifyReplace free-text crop with Paddy/Vaikool dynamic formsrc/pages/Workers.jsxMajor ExpandAdd 2 tabs, multi-worker entry, workerNameâ†’workerId, filters, PDFsrc/pages/System.jsxNEWCombined module: Price Settings + System Settings + Reportssrc/App.jsxModifyRemove 3 routes, add /system, update /drivers, remove /salary-driverserver/index.jsModifyAdd 9 new PDF endpoints (reports + worker + driver salary)server/pdfGenerator.jsModifyAdd generateDriverSalaryPDF, generateWorkerReport, 5 report generatorssrc/services/storage.jsNo changeupdateRecord and deleteRecord already exist and are correct2. Driver Module ChangesC01Driver DashboardAdd Season+Year and Month+Year filtersCurrent State (from code)DriverDashboard.jsx has no filter state. All stats are computed by iterating every log and salary record in the collections unconditionally. The result is a single set of lifetime totals that grows forever and never reflects a specific period.Why This Matters for Rural UsersAgriculture in Tamil Nadu runs on seasons â€” Kuruvai, Samba, Thaladi. A driver wants to know 'How much did I earn in the Samba 2025 season?' or 'How many hours did I work last month?' Lifetime totals answer neither question.New Logic â€” Two Filter ModesAdd a filter toggle at the top of DriverDashboard.jsx with two modes:Mode A â€” By Season (à®ªà®°à¯à®µà®®à¯ à®µà®¾à®°à®¿à®¯à®¾à®•)Season dropdown: KUR (à®•à¯à®±à¯à®µà¯ˆ) | SAM (à®šà®®à¯à®ªà®¾) | THA (à®¤à®²à®Ÿà®¿)Year input: 4-digit numberDate range per season:KUR = June 1 to September 30 of selected yearSAM = August 1 of selected year to January 31 of selected year+1THA = October 1 of selected year to January 31 of selected year+1Filter logs: log.date falls within the season date range AND log.driverId === userIdFilter salary: salary.date falls within the season date range AND salary.driverId === userIdMode B â€” By Month (à®®à®¾à®¤à®®à¯ à®µà®¾à®°à®¿à®¯à®¾à®•)Month dropdown: January to DecemberYear input: 4-digit numberFilter logs: new Date(log.date).getMonth()+1 === selectedMonth && getFullYear() === selectedYearFilter salary: same logic on salary.dateComputed Stats (same cards, filtered data)Stat CardComputationTamil LabelTotal HoursfilteredLogs.reduce((sum,l)=>sum+l.totalHours,0)à®®à¯Šà®¤à¯à®¤ à®®à®£à®¿ à®¨à¯‡à®°à®®à¯Days Workednew Set(filteredLogs.map(l=>l.date)).sizeà®µà¯‡à®²à¯ˆ à®¨à®¾à®Ÿà¯à®•à®³à¯Total EarnedfilteredSalary.reduce((sum,s)=>sum+s.netPay,0)à®šà®®à¯à®ªà®¾à®¤à®¿à®¤à¯à®¤à®¤à¯Total ReceivedfilteredSalary.reduce sumPayments across all payments[]à®ªà¯†à®±à¯à®±à®¤à¯Pending SalaryTotal Earned minus Total Receivedà®¨à®¿à®²à¯à®µà¯ˆ à®šà®®à¯à®ªà®³à®®à¯Default StateOn first load: Mode B (By Month), current month and year. Driver sees their current month's stats immediately without any interaction.C02Driver EntryFix session time defaults â€” empty inputs, correct triggerRoot Cause in CodeDriverEntry.jsx initialises sessions as:sessions: [{ start: '09:00', end: '10:00', durationHours: 1 }]This means every new log entry already has 1 hour pre-calculated. A driver who does not notice the pre-filled times and saves immediately will create an incorrect log entry. After a save, the reset also restores the same pre-filled defaults. There is no indication to the driver that they must change these values.The Calculation Trigger (How It Works Now)The handleSessionChange function checks: if (start && end) â†’ calculate getDuration(). This logic is correct â€” it fires whenever either field changes as long as the other is non-empty. The only problem is the pre-filled values bypass this trigger visually.Fix â€” Two Lines Changed// BEFORE â€” in initial useState:sessions: [{ start: '09:00', end: '10:00', durationHours: 1 }]// AFTER â€” initial state:sessions: [{ start: '', end: '', durationHours: 0 }]// AFTER â€” reset after save:sessions: [{ start: '', end: '', durationHours: 0 }]// AFTER â€” addSession handler:setSessions(prev => [...prev, { start: '', end: '', durationHours: 0 }])With empty defaults, the duration shows 0.00 until the driver fills in both times. Once start and end are both non-empty, getDuration() fires and the result appears immediately without needing to touch any field a second time.C03Driver SalaryDate range filter + PDF export of salary historyContext â€” What Changes After C08C03 describes additions to the Salary sub-module within Drivers.jsx (Tab 2). The standalone SalaryDriver.jsx is deleted. All features described here are implemented inside the new Drivers.jsx Salary tab. See C08 for the full salary logic redesign.Date Range FilterAdd two date inputs: From Date (à®¤à¯Šà®Ÿà®•à¯à®• à®¤à¯‡à®¤à®¿) and To Date (à®®à¯à®Ÿà®¿à®µà¯ à®¤à¯‡à®¤à®¿)Both default to empty (show all records)Filter applied: salary.date >= fromDate && salary.date <= toDateClear Filter button resets both dates to emptyFiltered count shown: 'Showing 5 of 12 entries'Driver logged in: can view their own filtered records in read-only modeAdmin logged in: can view any driver's filtered recordsPDF Export of Salary Statement'Download Statement PDF' button appears below the filter controlsSends filtered salary records + driver info to backend endpointBackend endpoint: POST /api/pdf/driver-salary-statementPDF layout: Driver name and village at top, period shown (from date to to date or 'All Records')Table rows: Date | Base | Bonus | Extra | Advance | Net Pay | Paid | BalanceFooter: Totals row + Grand Total OutstandingTamil headers on all columnsPDF filename: driver-name-salary-YYYY-MM.pdf3. Admin Module ChangesC04System SettingsChange any driver's login PIN from admin settingsLocationThis feature lives inside the new System.jsx module â†’ System Settings (à®•à®£à®¿à®©à®¿ à®…à®®à¯ˆà®ªà¯à®ªà¯) tab, in a 'Driver PIN Management' section below the Admin PIN change section.Current StateAdmin PIN change was added in v3.0 and stored in localStorage('admin_pin'). But driver PINs can only be changed by editing the driver record inside the Drivers module â€” there is no dedicated PIN management interface and no way to see that a PIN exists without editing.New LogicSection header: 'Driver Login PIN (à®“à®Ÿà¯à®Ÿà¯à®¨à®°à¯ à®•à®Ÿà®µà¯à®šà¯à®šà¯Šà®²à¯)'Driver dropdown: select from all active drivers in rl_driversCurrent PIN display: masked as '****' (not revealed, only replaceable)New PIN input: 4-digit numeric input with validationSave button: calls updateRecord('rl_drivers', driverId, { pin: newPin })Success message in Tamil: 'PIN à®µà¯†à®±à¯à®±à®¿à®•à®°à®®à®¾à®• à®®à®¾à®±à¯à®±à®ªà¯à®ªà®Ÿà¯à®Ÿà®¤à¯'Validation: PIN must be exactly 4 digits and numeric â€” inline Tamil error if notSecurity BehaviourThe new PIN takes effect immediately on next driver login â€” no restart neededOld PIN is overwritten â€” there is no PIN historyAdmin can set all drivers to unique PINs or reset forgotten PINsC05ReportsPDF export button for each of the 5 report typesCurrent StateReports.jsx renders 5 report types as HTML tables. There is no PDF generation capability at all. The reports cannot be printed, shared, or saved in any structured format.LocationReports live inside the new System.jsx module â†’ Reports (à®…à®±à®¿à®•à¯à®•à¯ˆà®•à®³à¯) tab. The PDF buttons are added to each report's section.5 Report PDF Endpoints â€” Logic Per ReportReportEndpointData SentPDF ContentsMonthly SummaryPOST /api/pdf/report/monthly{ month, year, incomeRows, expenseRows, totals }Header: Month+Year. Two tables: Income by source, Expense by source. Bottom row: Net ProfitMachine-wisePOST /api/pdf/report/machine{ season, year, machineRows }Header: Season+Year. Table: Machine type, Job count, Total hours, Revenue, Expense, ProfitFarmer DuesPOST /api/pdf/report/farmer-dues{ farmerRows }Header: 'Outstanding Balances as of DATE'. Table: Farmer name, Village, Harvester due, Rental due, Total due. Sorted by total due descendingDriver PayrollPOST /api/pdf/report/driver-payroll{ month, year, driverRows }Header: Month+Year. Table: Driver name, Hours worked, Earned, Paid, BalanceSeasonalPOST /api/pdf/report/seasonal{ season, year, jobRows, totals }Header: Season+Year. Table: Bill ID, Farmer, Hours, Amount, Paid, Balance. Footer: Season totalsFrontend BehaviourEach report section has a 'Download PDF (PDF à®ªà®¤à®¿à®µà®¿à®±à®•à¯à®•à®®à¯)' buttonButton is disabled and greyed out until report data has been generated (filter applied and rendered)Button enabled only when the report table has at least one rowOn tap: sends current report data to the endpoint, receives PDF blob, auto-downloadsPDF filename includes report type and period: monthly-summary-2026-04.pdfC06ExpensesFix undefined labels + 3 source-specific category listsRoot Cause â€” Why 'undefined' AppearsEvery category object in Expenses.jsx is structured as { value, ta } with no en field. The SelectField component renders labels as:opt.label || `${opt.en} (${opt.ta})`// opt.en is undefined for every category// Result: 'undefined (à®‡à®¯à®¨à¯à®¤à®¿à®° à®ªà®°à®¾à®®à®°à®¿à®ªà¯à®ªà¯)'This affects every category dropdown on the expenses screen. All English labels show as 'undefined'.Root Cause â€” Why Same Categories for All SourcesA single CATEGORIES array is used regardless of the selected source. Maintenance and Spare Parts appear under Home Expenses. Food appears under Business. There is no semantic connection between source and categories.Fix â€” Add en Field + 3 Separate Category Maps// BEFORE (broken):const CATEGORIES = [  { value: 'maintenance', ta: 'à®‡à®¯à®¨à¯à®¤à®¿à®° à®ªà®°à®¾à®®à®°à®¿à®ªà¯à®ªà¯' },  // no en field  ...];// AFTER (correct):const CATEGORY_MAP = {  business: [    { value: 'maintenance',   en: 'Maintenance',    ta: 'à®‡à®¯à®¨à¯à®¤à®¿à®° à®ªà®°à®¾à®®à®°à®¿à®ªà¯à®ªà¯' },    { value: 'diesel',        en: 'Fuel / Diesel',  ta: 'à®Žà®°à®¿à®ªà¯Šà®°à¯à®³à¯ / à®Ÿà¯€à®šà®²à¯' },    { value: 'parts',         en: 'Spare Parts',    ta: 'à®‰à®¤à®¿à®°à®¿ à®ªà®¾à®•à®™à¯à®•à®³à¯' },    { value: 'mechanic',      en: 'Mechanic Cost',  ta: 'à®®à¯†à®•à¯à®•à®¾à®©à®¿à®•à¯ à®•à®Ÿà¯à®Ÿà®£à®®à¯' },    { value: 'driver_food',   en: 'Driver Food',    ta: 'à®“à®Ÿà¯à®Ÿà¯à®¨à®°à¯ à®‰à®£à®µà¯' },    { value: 'tax',           en: 'Tax / Insurance',ta: 'à®µà®°à®¿ / à®•à®¾à®ªà¯à®ªà¯€à®Ÿà¯' },    { value: 'other',         en: 'Other',          ta: 'à®‡à®¤à®°' },  ],  own_farm: [    { value: 'seeds',         en: 'Seeds',          ta: 'à®µà®¿à®¤à¯ˆà®•à®³à¯' },    { value: 'fertilizer',    en: 'Fertilizer',     ta: 'à®‰à®°à®®à¯' },    { value: 'pesticide',     en: 'Pesticide',      ta: 'à®ªà¯‚à®šà¯à®šà®¿à®•à¯à®•à¯Šà®²à¯à®²à®¿' },    { value: 'irrigation',    en: 'Irrigation',     ta: 'à®¨à¯€à®°à¯à®ªà¯à®ªà®¾à®šà®©à®®à¯' },    { value: 'labour',        en: 'Labour',         ta: 'à®•à¯‚à®²à®¿' },    { value: 'equip_rental',  en: 'Equipment Rental',ta:'à®‰à®ªà®•à®°à®£ à®µà®¾à®Ÿà®•à¯ˆ' },    { value: 'other',         en: 'Other',          ta: 'à®‡à®¤à®°' },  ],  home_expense: [    { value: 'food',          en: 'Food',           ta: 'à®‰à®£à®µà¯' },    { value: 'medical',       en: 'Medical',        ta: 'à®®à®°à¯à®¤à¯à®¤à¯à®µà®®à¯' },    { value: 'education',     en: 'Education',      ta: 'à®•à®²à¯à®µà®¿' },    { value: 'transport',     en: 'Transport',      ta: 'à®ªà¯‹à®•à¯à®•à¯à®µà®°à®¤à¯à®¤à¯' },    { value: 'utilities',     en: 'Utilities',      ta: 'à®®à®¿à®©à¯à®šà®¾à®°à®®à¯/à®¤à®£à¯à®£à¯€à®°à¯' },    { value: 'other',         en: 'Other',          ta: 'à®‡à®¤à®°' },  ],};// When source changes, reset category to first option of new source:const handleSourceChange = (newSource) => {  setFormData({...formData,    source: newSource,    category: CATEGORY_MAP[newSource][0].value  });};C07Own Farm IncomePaddy vs Vaikool (Straw) dynamic income formCurrent StateOwnFarmIncome.jsx stores harvestType as free-text (e.g. 'Paddy') alongside numberOfBags and pricePerBag. There is no Paddy Straw (Vaikool) option. The unit system is fixed to 'bags' even though paddy straw is counted in bundles (kattu). This does not match real agricultural recording practice.New Schema{  id:             'OFI001',  date:           '2026-04-20',  incomeSource:   'paddy',          // 'paddy' | 'vaikool'  // Used when incomeSource = 'paddy':  numberOfBags:   50,               // à®®à¯‚à®Ÿà¯à®Ÿà¯ˆà®•à®³à®¿à®©à¯ à®Žà®£à¯à®£à®¿à®•à¯à®•à¯ˆ  pricePerBag:    2200,             // à®’à®°à¯ à®®à¯‚à®Ÿà¯à®Ÿà¯ˆ à®µà®¿à®²à¯ˆ (â‚¹)  // Used when incomeSource = 'vaikool':  numberOfBundles: 0,              // à®•à®Ÿà¯à®Ÿà¯à®•à®³à®¿à®©à¯ à®Žà®£à¯à®£à®¿à®•à¯à®•à¯ˆ  pricePerBundle:  0,              // à®’à®°à¯ à®•à®Ÿà¯à®Ÿà¯ à®µà®¿à®²à¯ˆ (â‚¹)  // Always present (computed on save):  totalIncome:    110000,  description:    '',              // free-text description field kept  source:         'own_farm'}Dynamic Form BehaviourSource dropdown (first field): Paddy (à®¨à¯†à®²à¯) | Paddy Straw / Vaikool (à®µà¯ˆà®•à¯à®•à¯‹à®²à¯)When 'paddy' selected â†’ show: No. of Bags (à®®à¯‚à®Ÿà¯à®Ÿà¯ˆà®•à®³à¯), Price per Bag (à®’à®°à¯ à®®à¯‚à®Ÿà¯à®Ÿà¯ˆ à®µà®¿à®²à¯ˆ)When 'vaikool' selected â†’ show: No. of Bundles (à®•à®Ÿà¯à®Ÿà¯à®•à®³à¯), Price per Bundle (à®’à®°à¯ à®•à®Ÿà¯à®Ÿà¯ à®µà®¿à®²à¯ˆ)totalIncome auto-computes in both cases: quantity Ã— price (live display as user types)Description field remains below totalIncome in both modesList View ChangesShow incomeSource label in Tamil: à®¨à¯†à®²à¯ (for paddy) or à®µà¯ˆà®•à¯à®•à¯‹à®²à¯ (for vaikool)Show correct unit: 50 à®®à¯‚à®Ÿà¯à®Ÿà¯ˆà®•à®³à¯ (for paddy) or 120 à®•à®Ÿà¯à®Ÿà¯à®•à®³à¯ (for vaikool)Existing paddy records with old schema (harvestType field): treat numberOfBags and pricePerBag as-is, show as à®¨à¯†à®²à¯4. Drivers Module â€” Full Restructure (C08)C08DriversTwo sub-modules + salary logic fully decoupled from driver logsStructure ChangeDrivers.jsx becomes a tabbed module with two sub-modules. The standalone SalaryDriver.jsx route is removed. The App.jsx /salary-driver route is removed. The driver nav now shows 'My Salary' which renders Drivers.jsx with the salary tab pre-selected and restricted to the logged-in driver's userId.TabTamil NameWho Can AccessContentsTab 1: Driversà®“à®Ÿà¯à®Ÿà¯à®¨à®°à¯à®•à®³à¯Admin onlyExisting add/edit/view/delete driver logic â€” unchangedTab 2: Salaryà®šà®®à¯à®ªà®³à®®à¯Admin (full) + Driver (read-only own records)New salary logic â€” see belowCurrent Salary Logic â€” The ProblemThe current SalaryDriver.jsx scans for driver logs without a linkedSalaryId. If availableLogs.length === 0, it immediately blocks with an alert and prevents saving. This creates a hard dependency: the salary module cannot record ANY payment unless there are unpaid driver logs available. Real scenarios this breaks:Admin wants to give a Diwali bonus: zero logs unpaid â†’ blockedAdmin wants to record a â‚¹500 advance before any work starts â†’ blockedAdmin wants to note extra pay for a driver who helped with non-machine work â†’ blockedDriver worked but log entry was not created yet â†’ blockedNew Salary Logic â€” Fully Independent EntriesEach salary entry is a standalone financial record. There is no dependency on driver logs. Admin can create a salary entry for any combination of base, bonus, extra, and advance at any time.// New salary entry schema (saved to rl_driver_salary):{  id:           'DSL001',  driverId:     'D001',  date:         '2026-04-20',  baseSalary:   1200,    // 0 if not this entry type  bonus:        0,  extraAmount:  0,  advance:      0,  // netPay is computed on render, NOT stored:  // netPay = (baseSalary + bonus + extraAmount) - advance  notes:        '',  payments:     []       // partial payment tracking (same pattern as Harvester)}Form Validation RuleAt least one of baseSalary, bonus, extraAmount, or advance must be > 0. If all four are 0 and Save is tapped, show inline Tamil error: 'à®•à¯à®±à¯ˆà®¨à¯à®¤à®¤à¯ à®’à®°à¯ à®¤à¯Šà®•à¯ˆ à®¤à¯‡à®µà¯ˆ (At least one amount is required)'. All four can be filled simultaneously in one entry â€” this is the normal combined salary entry. Any single field can be filled alone â€” this allows bonus-only, advance-only, or extra-only entries.Net Pay DisplayNet Pay is shown live as the user types: (baseSalary + bonus + extraAmount) - advance. If the result is negative (advance exceeds total earnings), show: 'à®®à¯à®©à¯à®ªà®£à®®à¯ à®šà®®à¯à®ªà®³à®¤à¯à®¤à¯ˆ à®µà®¿à®Ÿ à®…à®¤à®¿à®•à®®à®¾à®• à®‰à®³à¯à®³à®¤à¯' with a red background. Negative net pay is allowed to save â€” it represents a carry-forward advance situation.Payments[] Tracking (Same Pattern as Harvester)After a salary record is saved, admin can record partial payments against it using the PaymentHistory component (already built and working). This is the same pattern used in Harvester jobs and Rental entries. sumPayments(salary.payments) gives total paid. balance = netPay - sumPayments. Driver can view balance but cannot add payments.Edit and Delete â€” Admin OnlyActionAdminDriverRuleView salary recordsAll driversOwn records onlyDriver sees read-only list filtered by their userIdEdit salary entryYes â€” full edit of all fieldsNoEdit updates the record in rl_driver_salary via updateRecord()Delete salary entryYesNodeleteRecord() removes from rl_driver_salary; show ConfirmDialog firstAdd payment to salaryYesNoExisting addPayment() function in storage.js usedEdit or delete a paymentNoNoPayments are immutable; add a correction entry insteadLinking to Logs (Optional Context, Not Required)The old system used linkedSalaryId on driver logs to mark them as processed. This hard dependency is removed. However, the admin may optionally add a note on a salary entry referencing specific log dates (using the notes field). No automated log-linking is implemented in v3.1 â€” salary entries are fully independent.App.jsx Route Changes// REMOVE this route:<Route path='/salary-driver' element={<SalaryDriver />} />// UPDATE the driver nav link: instead of '/salary-driver'// Driver user sees: 'My Salary' â†’ navigates to /drivers?tab=salary// The Drivers.jsx tab parameter detects ?tab=salary on mount// and pre-selects Tab 2, filters to userId, and sets isReadOnly=true// OR simpler approach:// In App.jsx driver nav, render a link to /drivers with salary tab// In Drivers.jsx useEffect: if user.role === 'driver', auto-select salary tab//   and set isReadOnly = true, driverFilter = user.id5. Workers Module â€” Full Restructure (C09 + C12)C09WorkersTwo sub-modules + multi-worker entry + filters + PDF reportsStructure ChangeWorkers.jsx becomes a tabbed module with two sub-modules.TabTamil NameContentsTab 1: Workersà®µà¯‡à®²à¯ˆà®¯à®¾à®Ÿà¯à®•à®³à¯Add, edit, view, deactivate workers. Name, phone, village fields.Tab 2: Work Recordsà®µà¯‡à®²à¯ˆ à®ªà®¤à®¿à®µà¯Work entry form + filtered list + PDF reportsC12 â€” Multi-Worker Entry (Same Work, Same Day)This change comes from the real-world pattern where large agricultural tasks (fertilizing, transplanting seedlings â€” naatru nattal, kalai yeduthal, andai vettuthal) are done by multiple workers simultaneously. Recording them one by one is time-consuming and error-prone.Current StateWorkers.jsx saves one entry per form submission with a workerName string. To record 5 workers doing the same work, admin must fill the form 5 separate times. This is the root pain point.New Multi-Worker Entry Logic// Form state for multi-worker entry:const [formData, setFormData] = useState({  date: today,  selectedWorkerIds: [],   // ARRAY â€” multiple workers can be selected  workType: '',  baseSalary: 0,  bonus: 0,  extraAmount: 0,  advance: 0,  notes: ''});// Worker selection UI:// Show all active workers as a checklist (not a dropdown)// Each worker: [checkbox] Kumar  [checkbox] Murugan  [checkbox] Ravi// Admin ticks any combination of workers// Net Pay display: same formula, computed liveconst netPayable = (baseSalary + bonus + extraAmount) - advance;// On save â€” create ONE entry per selected worker:const handleSubmit = () => {  if (formData.selectedWorkerIds.length === 0) {    setError('à®•à¯à®±à¯ˆà®¨à¯à®¤à®¤à¯ à®’à®°à¯ à®µà¯‡à®²à¯ˆà®¯à®¾à®³à¯ à®¤à¯‡à®°à¯à®µà¯ à®šà¯†à®¯à¯à®¯à®µà¯à®®à¯');    return;  }  formData.selectedWorkerIds.forEach(workerId => {    const entry = {      id: generateId('rl_work_entries'),      workerId,            // reference â€” NOT workerName string      date: formData.date,      workType: formData.workType,      baseSalary:   parseFloat(formData.baseSalary)   || 0,      bonus:        parseFloat(formData.bonus)        || 0,      extraAmount:  parseFloat(formData.extraAmount)  || 0,      advance:      parseFloat(formData.advance)      || 0,      // netPayable NOT stored â€” computed on render      notes: formData.notes    };    addRecord('rl_work_entries', entry);  });  // Show: 'X workers saved successfully'  setSuccess(`${formData.selectedWorkerIds.length} à®µà¯‡à®²à¯ˆà®¯à®¾à®Ÿà¯à®•à®³à¯ à®šà¯‡à®®à®¿à®•à¯à®•à®ªà¯à®ªà®Ÿà¯à®Ÿà®©à®°à¯`);};workerName â†’ workerId MigrationAll new entries save workerId (reference to rl_workers). The display always looks up the worker name dynamically from rl_workers using the workerId. For historical entries that have workerName (string) instead of workerId, the list renders the stored workerName directly with a visual indicator ('Legacy entry') so they remain readable.Work Records FiltersFilterUI ElementLogicWorker filterDropdown: All Workers / select oneentries.filter(e => filter==='all' || e.workerId===selectedId)Date rangeFrom Date + To Date inputsentries.filter(e => e.date >= from && e.date <= to)Work type filterDropdown: All Types / select from workTypes.jsentries.filter(e => filter==='all' || e.workType===selected)CombinedAll 3 filters applied simultaneouslyAND logic â€” all conditions must matchPDF Reports â€” 3 TypesReport Type 1 â€” Individual Worker ReportSelect one worker from dropdownOptional: apply date range filter'Download PDF' â†’ POST /api/pdf/worker/individualPDF: Worker name, village, phone at top. Table: Date, Work Type, Base, Bonus, Extra, Advance, Net Pay. Footer: Totals rowReport Type 2 â€” Filtered ReportApply any combination of filters (worker, date, work type)'Download Filtered PDF' â†’ POST /api/pdf/worker/filteredPDF: Filter criteria shown at top. Table: Worker name, Date, Work Type, Net Pay. Footer: Total workers, Total amountReport Type 3 â€” Overall ReportNo filter required'Download Overall PDF' â†’ POST /api/pdf/worker/overallPDF: Summary table with one row per worker. Columns: Worker Name, Total Days, Total Earned. Grand total at bottom.6. System Module â€” New Combined Navigation (C10)C10NavigationMerge PricingConfig + Settings + Reports into one System moduleWhy This ChangeThe current navigation has three separate items: Config, Settings, Reports. These are rarely-used administrative functions that do not belong in the same nav level as operational modules like Harvester and Rental. Grouping them into a single 'System' module reduces nav clutter and groups administrative concerns together.New File: src/pages/System.jsxSystem.jsx is a new page with 3 tabs. Each tab renders the full content of the corresponding existing page â€” the existing pages are not deleted, they are imported as sub-components inside System.jsx.TabTamil NameContent SourceURL (tab param)Price Settingsà®µà®¿à®²à¯ˆ à®…à®®à¯ˆà®ªà¯à®ªà¯PricingConfig.jsx content/system?tab=priceSystem Settingsà®•à®£à®¿à®©à®¿ à®…à®®à¯ˆà®ªà¯à®ªà¯Settings.jsx content + Driver PIN from C04/system?tab=settingsReportsà®…à®±à®¿à®•à¯à®•à¯ˆà®•à®³à¯Reports.jsx content + PDF buttons from C05/system?tab=reportsApp.jsx Route Changes// REMOVE these 3 routes and nav links:// <Route path='/pricing-config' element={<PricingConfig />} />// <Route path='/settings'       element={<Settings />} />// <Route path='/reports'        element={<Reports />} />// ADD this 1 route:<Route path='/system' element={<System />} />// Admin nav: remove 3 items, add 1 'System (à®…à®®à¯ˆà®ªà¯à®ªà¯)' item// Default tab when navigating to /system: Price Settings// Deep links still work via tab param:// /system?tab=reports â†’ opens Reports tab directly// Useful for bookmarking or direct navigation from dashboard7. Admin Edit and Delete Driver Log Entries (C11)C11Driver LogsAdmin can edit or delete any driver log entry with cascade guardWhy This Is NeededDrivers enter logs in the field, often on small mobile screens. Mistakes happen â€” wrong farmer selected, wrong machine type, incorrect time. Currently there is no way to fix a log after it is saved. The admin has no edit or delete capability on driver log entries. This creates permanent incorrect data in the system.Where This Feature AppearsIn DriverEntry.jsx, the 'Today's Logs' section at the bottom of the page already shows saved logs. The admin view of this page should show ALL logs (not just today's), with edit and delete buttons per entry. Drivers see their own logs as before (today's only, no edit/delete).Edit LogicAdmin taps 'Edit (à®¤à®¿à®°à¯à®¤à¯à®¤à¯)' on any log entryThe log data populates the form at the top of the page (all fields: driver, farmer, machine type, date, sessions, diesel)The form shows in 'Edit Mode' with a header: 'Editing Log: LOG001'Admin makes corrections and taps 'Update Log (à®ªà®¤à®¿à®µà¯ˆ à®ªà¯à®¤à¯à®ªà¯à®ªà®¿)'updateRecord('rl_driver_logs', log.id, updatedLog) â€” updates in localStorageCascade on Edit â€” Linked Harvester JobIf the log being edited is linked to a Harvester Job (its id appears in any job's linkedLogIds array), the system must recalculate that job's totalHours and derived fields after the edit:// After updating a driver log:const linkedJob = getData('rl_harvester_jobs').find(j =>  j.linkedLogIds.includes(updatedLog.id));if (linkedJob) {  // Recompute totalHours from all linked logs  const allLinkedLogs = getData('rl_driver_logs')    .filter(l => linkedJob.linkedLogIds.includes(l.id));  const newTotalHours = allLinkedLogs.reduce((sum,l)=>sum+l.totalHours,0);  const newGross = newTotalHours * linkedJob.ratePerHour;  const newFinal = Math.max(0, newGross - linkedJob.discount);  const newDieselFromLogs = allLinkedLogs.reduce((sum,l)=>sum+(l.diesel?.total||0),0);  updateRecord('rl_harvester_jobs', linkedJob.id, {    totalHours:     newTotalHours,    grossAmount:    newGross,    finalAmount:    newFinal,    dieselFromLogs: newDieselFromLogs,    // totalExpense and netProfit are recomputed: final - (diesel + additionalDiesel + otherExp)  });}Delete LogicGuard â€” Cannot Delete if Log is Linked to a Harvester Job with Paymentsconst handleDeleteLog = (logId) => {  const linkedJob = getData('rl_harvester_jobs').find(j =>    j.linkedLogIds.includes(logId)  );  if (linkedJob) {    const hasPaid = linkedJob.payments && linkedJob.payments.length > 0;    if (hasPaid) {      // BLOCK DELETE â€” show Tamil error      setError('à®‡à®¨à¯à®¤ à®ªà®¤à®¿à®µà¯ à®’à®°à¯ à®šà¯†à®²à¯à®¤à¯à®¤à®ªà¯à®ªà®Ÿà¯à®Ÿ à®…à®±à¯à®µà®Ÿà¯ˆ à®µà¯‡à®²à¯ˆà®¯à¯à®Ÿà®©à¯ à®‡à®£à¯ˆà®•à¯à®•à®ªà¯à®ªà®Ÿà¯à®Ÿà¯à®³à¯à®³à®¤à¯.');      setError('(This log is linked to a harvester job that has payments recorded.)');      return;    } else {      // Warn but allow â€” show ConfirmDialog with warning      setConfirmMsg('à®‡à®¨à¯à®¤ à®ªà®¤à®¿à®µà¯ˆ à®¨à¯€à®•à¯à®•à¯à®µà®¤à¯ à®…à®±à¯à®µà®Ÿà¯ˆ à®µà¯‡à®²à¯ˆà®¯à®¿à®²à®¿à®°à¯à®¨à¯à®¤à¯ à®¤à¯Šà®Ÿà®°à¯à®ªà¯ˆ à®…à®•à®±à¯à®±à¯à®®à¯. à®¤à¯Šà®Ÿà®°à®µà®¾?');      setPendingDeleteId(logId);      return;    }  }  // No linked job â€” standard confirm + delete  setConfirmMsg('à®‡à®¨à¯à®¤ à®ªà®¤à®¿à®µà¯ˆ à®¨à¯€à®•à¯à®• à®µà®¿à®°à¯à®®à¯à®ªà¯à®•à®¿à®±à¯€à®°à¯à®•à®³à®¾?');  setPendingDeleteId(logId);};// On confirm:const handleConfirmDelete = () => {  deleteRecord('rl_driver_logs', pendingDeleteId);  // Also unlink from any job that referenced this log (if no payments):  const jobs = getData('rl_harvester_jobs');  jobs.forEach(job => {    if (job.linkedLogIds.includes(pendingDeleteId)) {      const newIds = job.linkedLogIds.filter(id => id !== pendingDeleteId);      updateRecord('rl_harvester_jobs', job.id, { linkedLogIds: newIds });      // Recompute job totalHours with remaining logs    }  });};Admin Log List ViewWhen the admin visits DriverEntry.jsx, the bottom section shows ALL logs (not just today's). Filters available: Driver filter, Date range, Machine type. Each log card shows Edit and Delete buttons. Drivers see their own logs (today only) with no edit/delete buttons â€” same as current behaviour.8. Backend Changes â€” New PDF Endpoints8.1  All New Endpoints in server/index.jsEndpointMethodInputGenerator FunctionPOST /api/pdf/driver-salary-statementPOST{ driver, salaryRecords, fromDate, toDate }generateDriverSalaryStatement()POST /api/pdf/worker/individualPOST{ worker, entries }generateWorkerIndividualReport()POST /api/pdf/worker/filteredPOST{ entries, filters }generateWorkerFilteredReport()POST /api/pdf/worker/overallPOST{ summary }generateWorkerOverallReport()POST /api/pdf/report/monthlyPOST{ month, year, incomeRows, expenseRows }generateMonthlyReport()POST /api/pdf/report/machinePOST{ season, year, rows }generateMachineReport()POST /api/pdf/report/farmer-duesPOST{ rows, asOfDate }generateFarmerDuesReport()POST /api/pdf/report/driver-payrollPOST{ month, year, rows }generateDriverPayrollReport()POST /api/pdf/report/seasonalPOST{ season, year, rows }generateSeasonalReport()8.2  PDF Layout Standard (All New Documents)Header: Document title in Tamil + English, date generated, filter/period infoFont: NotoSansTamil for all Tamil text, Helvetica for numbers and EnglishTable structure: Navy header row, alternating white/light-gray data rowsFooter: Page number, company name 'SmartUzhavan â€” à®•à®¿à®°à®¾à®® à®•à®£à®•à¯à®•à¯'Totals row: Bold, teal background, at the bottom of every tableAll monetary values: formatted as â‚¹ X,XXX (using existing formatCurrency logic)9. Implementation Build OrderFollow this exact order. Each step has dependencies on the previous.Complete and test each step before moving to the next.Do NOT combine multiple steps in one response.StepChangeFile(s)DependencyEst. Time1Fix DriverEntry empty session defaults (C02)DriverEntry.jsxNone â€” isolated 2-line fix5 min2Fix Expenses undefined labels + 3 category maps (C06)Expenses.jsxNone â€” isolated data fix20 min3Fix OwnFarmIncome Paddy/Vaikool schema (C07)OwnFarmIncome.jsxNone â€” isolated form change25 min4Add Season+Month filter to DriverDashboard (C01)DriverDashboard.jsxNone â€” reads existing data30 min5Rebuild Workers.jsx with tabs + workerNameâ†’workerId (C09 base)Workers.jsxNone â€” self-contained40 min6Add multi-worker checklist entry to Workers.jsx (C12)Workers.jsxStep 5 must be done first20 min7Rebuild Drivers.jsx with tabs + new salary logic (C08)Drivers.jsx + App.jsxSteps 5,6 done (pattern reference)60 min8Add date filter + PDF button to Salary tab (C03)Drivers.jsx + serverStep 7 must be done first30 min9Add Driver PIN change to future System module (C04)Drivers.jsx temp locationStep 7 must be done first15 min10Add Admin log edit/delete with cascade guard (C11)DriverEntry.jsx + storageStep 1 must be done first40 min11Create System.jsx â€” merge Config+Settings+Reports (C10)System.jsx + App.jsxSteps 4,7,8,9 done first30 min12Add PDF buttons to Reports tab in System.jsx (C05)System.jsx + serverStep 11 must be done first60 min13Add 9 new PDF endpoints + generator functions (C03,C05,C09)server/index.js + pdfGenerator.jsSteps 8,10,12 must be done first90 min14End-to-end test: all roles, all modules, all PDF typesAllAll steps complete30 min10. Do Not Touch â€” Working FilesThese files are verified working in v3.0. Modifying them without explicit instruction will introduce new bugs.If a fix in this plan requires importing from these files, import â€” do NOT modify.FileStatussrc/services/storage.jsâœ” All 7 functions correct â€” updateRecord and deleteRecord ready for C11src/services/calculations.jsâœ” All 9 functions correctsrc/services/billId.jsâœ” HB-SSS-YYYY-NNN format correctsrc/services/pdfService.jsâœ” Correct (API URL env var fix from v3.0 already applied)src/utils/timeUtils.jsâœ” Overnight guard correctsrc/utils/idGenerator.jsâœ” All prefixes correctsrc/data/machineTypes.js, seasons.js, workTypes.jsâœ” All correctsrc/pages/Harvester.jsxâœ” Fully working â€” do not touchsrc/pages/Rental.jsxâœ” Working â€” PDF fix from v3.0 appliedsrc/pages/FarmerView.jsxâœ” Workingsrc/pages/Finance.jsxâœ” Working (lending tab)src/pages/Reports.jsxâœ” Working (PDF buttons added in C05 â€” do not change table logic)src/pages/Login.jsxâœ” Working â€” PIN security fix from v3.0 appliedsrc/pages/PricingConfig.jsxâœ” Working â€” will be imported into System.jsxsrc/pages/Settings.jsxâœ” Working â€” will be imported into System.jsxsrc/components/ (all)âœ” All components working â€” reuse PaymentHistory for Salaryserver/pdfGenerator.js (existing functions)âœ” generateHarvesterBill, generateStatement, generateRentalReceipt workingEND OF IMPLEMENTATION PLAN â€” VERSION 3.1SmartUzhavan â€” 12 Changes  |  8 Modified Files  |  3 New Files  |  9 New PDF Endpoints
